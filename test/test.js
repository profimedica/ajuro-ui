'use strict';

var expect = require('chai').expect;
var ajuroUI = require('../main');

describe('#ajuroUI', function() {
    it('should convert single digits', function() {
        // var result = ajuroUI.list(1);
        // expect(1).to.equal(1);
    });
});