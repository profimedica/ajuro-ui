// nib is important, as it gets stylus to include variations on the CSS for browsers.
// eg 'user-select' gets compiled to versions '-webkit-user-select, -moz-user-select, -ms-user-select'
@import 'nib'

// if we don't do this, then the width and height of the grid would be ignored,
// as there is no default display for the element ajuro-a2 (as it's not a natural dom element)
ajuro-angular
    display inline-block

ajuro-ng2
    display inline-block

ajuro
    display block

ajuro-polymer
    display block

ajuro-aurelia
    display block

.aj-rtl
    //text-align right
    direction rtl

.aj-ltr
    //text-align left
    direction ltr

.aj-select-agg-func-popup
    position absolute

// when we drag the mouse in ajuro, this class gets added / removed from the body, so that
// the mouse isn't selecting text when dragging.
.aj-body-no-select
    user-select none

.aj-root
    /* set to relative, so absolute popups appear relative to this */
    position relative
    box-sizing border-box
    /* was getting some 'should be there' scrolls, this sorts it out */
    overflow hidden

.aj-layout-normal .aj-root
    height 100%

.aj-font-style
    cursor default
    /* disable user mouse selection */
    user-select none

.aj-layout-for-print
    white-space nowrap
    display inline-block

.aj-layout-normal
    height 100%

.aj-layout-auto-height {}

.aj-popup-backdrop
    position fixed
    left 0px
    top 0px
    width 100%
    height 100%

.aj-header
    white-space nowrap
    box-sizing border-box
    overflow hidden
    width 100%

.aj-layout-normal .aj-header
    position absolute
    top 0px
    left 0px

.aj-pinned-left-header
    float left
    box-sizing border-box
    display inline-block
    overflow hidden
    height 100%

.aj-pinned-right-header
    float right
    box-sizing border-box
    display inline-block
    overflow hidden
    height 100%

.aj-header-viewport
    box-sizing border-box
    overflow hidden
    height 100%

.aj-layout-normal .aj-header-row
    position absolute

.aj-layout-normal .aj-header-container
    box-sizing border-box
    position relative
    white-space nowrap
    height 100%

.aj-layout-auto-height .aj-header-row
    position absolute

.aj-layout-auto-height .aj-header-container
    box-sizing border-box
    position relative
    white-space nowrap
    height 100%

.aj-layout-for-print .aj-header-container
    white-space nowrap

.aj-header-overlay
    display block
    position absolute

.aj-header-cell
    box-sizing border-box
    vertical-align bottom
    text-align center
    display inline-block
    height 100%
    position absolute
    //float left // safari doesn't align if we don't float

.aj-floating-filter
    box-sizing border-box
    position absolute
    display inline-block

.aj-floating-filter-body
    margin-right 25px
    height: 20px;

.aj-floating-filter-full-body
    width:100%;
    height: 20px;

.aj-floating-filter-button
    width: 25px
    height: 20px
    float: right
    margin-top: -20px
    button
        width: 25px;
        height:19px
        padding: 0;

.aj-floating-filter-input
    width: 100%

.aj-floating-filter-input:read-only
    background-color #eeeeee

.aj-floating-filter-menu
    position absolute
    user-select none

// the ghost header cell doesn't live inside the grid, it's parent is the body element,
// so we cannot style using standard themes. hence we break the rules, and style it here
.aj-dnd-ghost
    font-size 14px
    font-family "Helvetica Neue",Helvetica,Arial,sans-serif;
    position absolute
    background #e5e5e5
    border 1px solid black
    cursor move
    user-select none
    box-sizing border-box
    overflow hidden
    text-overflow ellipsis
    padding 3px
    line-height 1.4
    //img
    //    vertical-align: middle;
    //    border: 0px;

.aj-dnd-ghost-icon
    display inline-block
    float left
    padding-left 2px
    padding-right 2px

.aj-dnd-ghost-label
    display inline-block

.aj-header-group-cell
    height 100%
    display inline-block
    box-sizing border-box
    text-overflow ellipsis
    overflow hidden
    position absolute
    //float: left // safari doesn't align if we don't float

// we needed to have display of table-cell below,
// as with inline-block, the layout messed up in Safari (not problems in the others).
.aj-header-group-cell-label
    //display table-cell
    text-overflow ellipsis
    overflow hidden

.aj-header-cell-label
    text-overflow ellipsis
    overflow hidden

.aj-header-cell-resize
    height 100%
    width 4px
    cursor col-resize

.aj-ltr .aj-header-cell-resize
    float right

.aj-ltr .aj-pinned-right-header .aj-header-cell-resize
    float left

.aj-rtl .aj-header-cell-resize
    float left

.aj-rtl .aj-pinned-left-header .aj-header-cell-resize
    float right

.aj-ltr .aj-header-select-all
    float left

.aj-rtl .aj-header-select-all
    float right

.aj-header-expand-icon
    padding-left 4px

.aj-header-cell-menu-button
    float right

.aj-overlay-panel
    display table
    width 100%
    height 100%
    pointer-events none

.aj-overlay-wrapper
    display table-cell
    vertical-align middle
    text-align center

.aj-bl-overlay
    pointer-events none
    position absolute
    height 100%
    width: 100%
    top 0px
    left 0px

.aj-overlay-loading-center {}

.aj-bl-full-height
    height 100%
    overflow auto
    position relative

.aj-bl-west
    float left

.aj-bl-full-height-west
    height 100%

.aj-bl-east
    float right

.aj-bl-full-height-east
    height 100%

.aj-bl-full-height-center
    height 100%

.aj-bl-normal
    height 100%
    position relative

.aj-bl-normal-center-row
    height 100%
    overflow hidden

.aj-bl-normal-west
    height 100%
    float left

.aj-bl-normal-east
    height 100%
    float right

.aj-bl-normal-center
    height 100%

.aj-bl-dont-fill
    position relative

.aj-body
    width 100%
    box-sizing border-box

.aj-layout-normal .aj-body
    height 100%
    position absolute

.aj-floating-top
    width 100%
    white-space nowrap
    box-sizing border-box
    overflow hidden

.aj-layout-normal .aj-floating-top
    position absolute
    left 0px

.aj-pinned-left-floating-top
    float left
    box-sizing border-box
    display inline-block
    overflow hidden
    position relative // otherwise the % width of children is not relative to this element

.aj-layout-normal .aj-pinned-left-floating-top
    height 100%

.aj-pinned-right-floating-top
    float right
    box-sizing border-box
    display inline-block
    overflow hidden
    position relative // otherwise the % width of children is not relative to this element

.aj-layout-normal .aj-pinned-right-floating-top
    height 100%

.aj-floating-top-viewport
    box-sizing border-box
    overflow hidden

.aj-layout-normal .aj-floating-top-viewport
    height 100%

.aj-floating-top-container
    box-sizing border-box
    position relative
    white-space nowrap
    //height: 100%

.aj-floating-bottom
    width 100%
    white-space nowrap
    box-sizing border-box
    overflow hidden

.aj-layout-normal .aj-floating-bottom
    position absolute
    left 0px

.aj-pinned-left-floating-bottom
    float left
    box-sizing border-box
    display inline-block
    overflow hidden
    position relative // otherwise the % width of children is not relative to this element

.aj-layout-normal .aj-pinned-left-floating-bottom
    height 100%

.aj-pinned-right-floating-bottom
    float right
    box-sizing border-box
    display inline-block
    overflow hidden
    position relative // otherwise the % width of children is not relative to this element

.aj-layout-normal .aj-pinned-right-floating-bottom
    height 100%

.aj-floating-bottom-viewport
    box-sizing border-box
    overflow hidden

.aj-layout-normal .aj-floating-bottom-viewport
    height 100%

.aj-floating-bottom-container
    box-sizing border-box
    position relative
    white-space nowrap
    //height 100%

.aj-pinned-left-cols-viewport
    float left

.aj-pinned-left-cols-container
    display inline-block
    position relative

.aj-pinned-right-cols-viewport
    float right

// for LTR, we show scrolls on the right pinned only
.aj-ltr .aj-pinned-right-cols-viewport
    overflow-x hidden
    overflow-y auto

.aj-ltr .aj-pinned-left-cols-viewport
    overflow hidden

// for RTL, other way around, we show scrolls on left only
.aj-rtl .aj-pinned-right-cols-viewport
    overflow hidden

.aj-rtl .aj-pinned-left-cols-viewport
    overflow-x hidden
    overflow-y auto

.aj-pinned-right-cols-container
    display inline-block
    position relative

.aj-layout-normal .aj-body-viewport-wrapper
    height 100%

.aj-body-viewport
    overflow-x auto
    overflow-y auto // if we are pinning to the right, overflow y will be hidden

.aj-layout-normal .aj-body-viewport
    height 100%

.aj-full-width-viewport
    height 100%
    width 100%
    // we want this panel to be on top of the others
    position absolute
    top 0px
    left 0px
    display inline
    // because the full width container is an overlay over the rest of the grid, we cannot
    // have it consume mouse events, otherwise no mouse events would be allowed on the
    // rest of the grid
    pointer-events none
    box-sizing border-box
    overflow hidden

.aj-full-width-container
    // if we don't put this in, the RHS will overflow into the viewports margin,
    // no idea why, trial and error
    overflow hidden
    position relative
    width 100%

.aj-floating-bottom-full-width-container
    height 100%
    width 100%
    position absolute
    top 0px
    left 0px
    pointer-events none
    overflow hidden
    display inline

.aj-floating-top-full-width-container
    height 100%
    width 100%
    position absolute
    top 0px
    left 0px
    pointer-events none
    overflow hidden
    display inline

.aj-full-width-row
    // turn events back on, as we removed them in the parent
    pointer-events all
    overflow hidden

.aj-layout-normal .aj-body-container
    position relative
    display inline-block

.aj-layout-auto-height .aj-body-container
    position relative
    display inline-block

// for animations, allows rows to slide up and down,
.aj-row-animation
    transition top 0.4s, height 0.4s, background-color 0.1s, opacity 0.2s

.aj-row-no-animation
    transition background-color 0.1s

.aj-row
    box-sizing border-box

.aj-layout-normal .aj-row
    white-space nowrap
    position absolute
    width 100%

.aj-layout-auto-height .aj-row
    white-space nowrap
    position relative // height relative so that the cell takes the height when set to 100%
    width 100%

.aj-layout-for-print .aj-row
//    white-space: nowrap
    position relative // need at least some position for the cell height to work

.aj-column-moving
    .aj-cell
        transition left 0.2s
    .aj-header-cell
        transition left 0.2s
    .aj-header-group-cell
        transition left 0.2s, width 0.2s

.aj-column-drop
    width 100%
    box-sizing border-box

.aj-column-drop-vertical

    .aj-column-drop-cell
        display block

    .aj-column-drop-empty-message
        display block

    .aj-column-drop-cell-button
        line-height 16px

.aj-ltr .aj-column-drop-vertical .aj-column-drop-cell-button
    float right

.aj-rtl .aj-column-drop-vertical .aj-column-drop-cell-button
    float left

.aj-column-drop-horizontal
    white-space: nowrap;

    .aj-column-drop-cell
        display inline-block

    .aj-column-drop-empty-message
        display inline-block

.aj-cell
    display inline-block
    white-space nowrap
    height 100%
    box-sizing border-box
    text-overflow ellipsis
    overflow hidden
    // this float was needed for layout in safari, without it, when images were used (eg country flags),
    // then the layout was broken
    //float: left
    position absolute

// this is used by the animateShowChangeCellRenderer. it is arguable that this belongs in the themes,
// however it is not tied to color, only placement and visiblity, which is behaviour and not style,
// thus belongs here, besides it doesn't change wih the themes
.aj-value-slide-out
    opacity 1.0
    margin-right 5px
    transition opacity 3s, margin-right 3s // as value fades, it also moves to the left via the margin setting
    transition-timing-function linear

.aj-value-slide-out-end
    opacity 0.0
    margin-right 10px

.aj-opacity-zero
    opacity 0.0

.aj-cell-edit-input
    width 100%
    height 100%

.aj-group-cell-entire-row
    //position: absolute // took this out because of https://github.com/ceolter/angular-grid/issues/115
    width: 100%
    display inline-block
    white-space nowrap
    height 100%
    box-sizing border-box
    text-overflow ellipsis
    overflow hidden

.aj-footer-cell-entire-row
    //position: absolute // took this out because of https://github.com/ceolter/angular-grid/issues/115
    width 100%
    display inline-block
    white-space nowrap
    height 100%
    box-sizing border-box
    text-overflow ellipsis
    overflow hidden

.aj-large .aj-root
    font-size 20px

.aj-popup-editor
    position absolute
    user-select none

.aj-menu
    position absolute
    user-select none

.aj-menu-column-select-wrapper
    width 200px
    height 300px
    overflow auto

.aj-menu-list
    display table
    border-collapse collapse

.aj-menu-option
    display table-row

.aj-menu-option-text
    display table-cell

.aj-menu-option-shortcut
    display table-cell

.aj-menu-option-icon
    display table-cell

.aj-menu-option-popup-pointer
    display table-cell

.aj-menu-separator
    display table-row

.aj-menu-separator-cell
    display table-cell

.aj-filter {}
    //z-index: 100

.aj-virtual-list-viewport
    overflow-x auto
    height 100%
    width 100%

.aj-virtual-list-container
    position relative
    overflow hidden

.aj-rich-select
    outline none

.aj-rich-select-row
    white-space nowrap

.aj-rich-select-list
    width 200px
    height 200px

.aj-set-filter-list
    width 200px
    height 200px

.aj-set-filter-item
    text-overflow ellipsis
    overflow hidden
    white-space nowrap

.aj-virtual-list-item
    position absolute
    width 100%;

.aj-filter-filter
    width 170px
    margin 4px

.aj-floating-filter-body
    input
        width 100%
        margin 0
        height 19px

.aj-floating-filter-full-body
    input
        width 100%
        margin 0
        height 19px

.aj-filter-select
    width 110px
    margin 4px 4px 0px 4px

.aj-paging-panel {}

.aj-list-selection
    user-select none
    cursor default

.aj-tool-panel
    width 200px
    user-select none
    cursor default
    box-sizing border-box
    overflow auto

.aj-layout-normal .aj-tool-panel
    height 100%

.aj-column-select-indent
    display inline-block

.aj-column-select-column
    white-space nowrap

.aj-ltr .aj-column-select-column
    margin-left 14px

.aj-rtl .aj-column-select-column
    margin-right 14px

.aj-column-select-column-group
    white-space nowrap

//.aj-column-select-panel
//    height: 100%;
//    overflow: auto;

.aj-hidden
    display none !important

.aj-visibility-hidden
    visibility hidden !important

.aj-faded
    opacity 0.3

.aj-width-half
    width 50%
    display inline-block

//.aj-tool-panel-container
//    user-select: none;
//    cursor: default;
//    width: 200px;

// setting shake class to an item will give it a left ot right animation
// used for the 'left' and 'rigth' arrows when dragging columns and scrolling
.aj-shake-left-to-right
    animation-name aj-shake-left-to-right
    animation-duration 0.2s
    animation-iteration-count infinite
    animation-direction alternate

@keyframes aj-shake-left-to-right
    from
        padding-left 6px
        padding-right 2px
    to
        padding-left 2px
        padding-right 6px
